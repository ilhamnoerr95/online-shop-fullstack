{
  "name": "turbo pack full stack ",
  "workspaces": [
    "apps/*",
    "apps/*/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.6.3"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": "20.20.*",
    "yarn": "1.22.*"
  }
}
