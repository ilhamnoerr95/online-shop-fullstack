### PRISMA SCHEMA
1. put on all schema in 1 folder = pakcages, that make easier setup for schema db:
```bash
packages/
    prisma/
        prisma-commerce/
            schema.prisma
        prisma-profile/
            schema.prisma
        prisma-payment/
            schema.prisma
```